# `@benchguard/win32-x64`

Native BenchGuard binary package for x86-64 Windows. This package is selected
by `@benchguard/cli`; install the CLI package rather than depending on this
platform package directly. BenchGuard uses a Windows Job Object for managed
CPU, peak working-set memory, and timeout cleanup.
